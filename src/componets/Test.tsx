import React, { useState } from 'react';

import type { 타입이름 } from '../types/testType';
const Test = () => {
   const [temp, setTemp] = useState<타입이름>([]);
   return <div>est</div>;
};
export default Test;
